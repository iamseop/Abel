import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dyadtestabelmain',
  appName: 'dyad test Abel-main',
  webDir: 'dist'
};

export default config;
